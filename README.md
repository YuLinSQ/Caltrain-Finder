# Caltrain-Finder
Real-time transit tracker with location-based station discovery, built with React Native and REST APIs.  
(Uses your location to recommend closest stations, shows trains and arrival times based on station selected.)  
# Setup
npx create-expo-app@latest project-name
replace app folder with the files here
npx expo start --web --clear on a terminal (I used WSL in vscode)
