# Caltrain-Finder
Uses your location to recommend closest stations, shows trains and arrival times based on station selected.  
Note: run npx expo start --web --clear after placing this inside the app folder of an expo/react native web server
